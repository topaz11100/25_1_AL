#include "include.h"

template <typename T>
vector<vector<T>> floyd_min_D(const vector<vector<T>>& input)
{
    vector<vector<T>> D{ input };
    for (int k = 1; k < input.size(); k += 1)
    {
        for()
    }
}